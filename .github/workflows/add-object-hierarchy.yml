name: Add object hierarchy

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/add-object-hierarchy.yml'

permissions:
  contents: write

jobs:
  implement:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Implement parent-child pilgrimage objects
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path

          def replace_once(path, old, new):
              p = Path(path)
              text = p.read_text(encoding='utf-8')
              if old not in text:
                  raise SystemExit(f'Anchor not found in {path}: {old[:100]!r}')
              p.write_text(text.replace(old, new, 1), encoding='utf-8')

          migration = Path('database/migrations/2026_07_31_124500_add_parent_object_id_to_pilgrimage_objects_table.php')
          migration.write_text(r'''<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::table('pilgrimage_objects', function (Blueprint $table) {
            $table->foreignId('parent_object_id')
                ->nullable()
                ->after('object_type_id')
                ->constrained('pilgrimage_objects')
                ->nullOnDelete();

            $table->index(['parent_object_id', 'is_published']);
        });
    }

    public function down(): void
    {
        Schema::table('pilgrimage_objects', function (Blueprint $table) {
            $table->dropIndex(['parent_object_id', 'is_published']);
            $table->dropConstrainedForeignId('parent_object_id');
        });
    }
};
''', encoding='utf-8')

          # Model: self-referencing hierarchy and hierarchy-aware type scope.
          replace_once(
              'app/Models/PilgrimageObject.php',
              "        'object_type_id',\n",
              "        'object_type_id',\n        'parent_object_id',\n",
          )
          replace_once(
              'app/Models/PilgrimageObject.php',
              '''    public function objectType(): BelongsTo
    {
        return $this->belongsTo(ObjectType::class);
    }

''',
              '''    public function objectType(): BelongsTo
    {
        return $this->belongsTo(ObjectType::class);
    }

    public function parentObject(): BelongsTo
    {
        return $this->belongsTo(self::class, 'parent_object_id');
    }

    public function childObjects(): HasMany
    {
        return $this->hasMany(self::class, 'parent_object_id')
            ->orderBy('name');
    }

    public function publishedChildObjects(): HasMany
    {
        return $this->hasMany(self::class, 'parent_object_id')
            ->published()
            ->orderBy('name');
    }

''',
          )
          replace_once(
              'app/Models/PilgrimageObject.php',
              '''    public function scopeSearch(Builder $query, ?string $term): Builder
''',
              '''    public function scopeTypeOrParentOfType(Builder $query, ?string $slug): Builder
    {
        $slug = trim((string) $slug);

        if ($slug === '') {
            return $query;
        }

        return $query->where(function (Builder $query) use ($slug) {
            $query->whereHas('objectType', fn (Builder $query) => $query->where('slug', $slug))
                ->orWhereHas('publishedChildObjects', function (Builder $query) use ($slug) {
                    $query->whereHas('objectType', fn (Builder $query) => $query->where('slug', $slug));
                });
        });
    }

    public function scopeSearch(Builder $query, ?string $term): Builder
''',
          )

          # Search service used by API/admin: include object type and published children.
          replace_once(
              'app/Services/PilgrimageObjectSearchService.php',
              "use Illuminate\\Support\\Collection;\n\n",
              "",
          )
          replace_once(
              'app/Services/PilgrimageObjectSearchService.php',
              "                'id',\n                'name',\n",
              "                'id',\n                'object_type_id',\n                'name',\n",
          )
          replace_once(
              'app/Services/PilgrimageObjectSearchService.php',
              "            ->with('sanctities:id,name')\n",
              "            ->with([\n                'objectType:id,name,slug',\n                'sanctities:id,name',\n                'publishedChildObjects.objectType:id,name,slug',\n                'publishedChildObjects.sanctities:id,name',\n            ])\n",
          )
          replace_once(
              'app/Services/PilgrimageObjectSearchService.php',
              '''        $name = $this->normalize((string) $object->name);
        $sanctities = $this->normalize($object->sanctities->pluck('name')->implode(' '));
        $allText = $this->normalize(implode(' ', array_filter([
            $object->name,
            $object->address,
            $object->short_description,
            $object->description,
            $object->history,
            $object->schedule_text,
            $object->sanctities->pluck('name')->implode(' '),
        ])));
''',
              '''        $name = $this->normalize((string) $object->name);
        $type = $this->normalize(implode(' ', array_filter([
            $object->objectType?->name,
            $object->objectType?->slug,
        ])));
        $sanctities = $this->normalize($object->sanctities->pluck('name')->implode(' '));
        $children = $this->normalize($object->publishedChildObjects->map(function (PilgrimageObject $child) {
            return implode(' ', array_filter([
                $child->name,
                $child->objectType?->name,
                $child->objectType?->slug,
                $child->address,
                $child->short_description,
                $child->description,
                $child->history,
                $child->schedule_text,
                $child->sanctities->pluck('name')->implode(' '),
            ]));
        })->implode(' '));
        $allText = $this->normalize(implode(' ', array_filter([
            $object->name,
            $object->objectType?->name,
            $object->objectType?->slug,
            $object->address,
            $object->short_description,
            $object->description,
            $object->history,
            $object->schedule_text,
            $object->sanctities->pluck('name')->implode(' '),
            $children,
        ])));
''',
          )
          replace_once(
              'app/Services/PilgrimageObjectSearchService.php',
              '''            if ($variant !== '' && str_contains($sanctities, $variant)) {
                $score += 250;
            }

            $bestScore = max($bestScore ?? 0, $score);
''',
              '''            if ($variant !== '' && str_contains($type, $variant)) {
                $score += 300;
            }

            if ($variant !== '' && str_contains($sanctities, $variant)) {
                $score += 250;
            }

            if ($this->tokenMatchScore($variant, $children) !== null) {
                $score += 120;
            }

            $bestScore = max($bestScore ?? 0, $score);
''',
          )

          # Fuzzy website search: rank parents by their published children and types.
          replace_once(
              'app/Services/PilgrimageObjectFuzzySearch.php',
              '''                    $this->fieldScore($object->name, $queryVariants, 120),
                    $this->fieldScore($object->address, $queryVariants, 85),
''',
              '''                    $this->fieldScore($object->name, $queryVariants, 120),
                    $this->fieldScore($object->objectType?->name, $queryVariants, 115),
                    $this->fieldScore($object->objectType?->slug, $queryVariants, 105),
                    $this->fieldScore($object->address, $queryVariants, 85),
''',
          )
          replace_once(
              'app/Services/PilgrimageObjectFuzzySearch.php',
              '''                    $object->sanctities
                        ->map(fn ($sanctity) => $this->fieldScore($sanctity->name, $queryVariants, 105))
                        ->max() ?? 0,
''',
              '''                    $object->sanctities
                        ->map(fn ($sanctity) => $this->fieldScore($sanctity->name, $queryVariants, 105))
                        ->max() ?? 0,
                    $object->publishedChildObjects
                        ->map(function (PilgrimageObject $child) use ($queryVariants) {
                            return max(
                                $this->fieldScore($child->name, $queryVariants, 90),
                                $this->fieldScore($child->objectType?->name, $queryVariants, 100),
                                $this->fieldScore($child->objectType?->slug, $queryVariants, 90),
                                $this->fieldScore($child->address, $queryVariants, 60),
                                $this->fieldScore($child->short_description, $queryVariants, 55),
                                $child->sanctities
                                    ->map(fn ($sanctity) => $this->fieldScore($sanctity->name, $queryVariants, 75))
                                    ->max() ?? 0,
                            );
                        })
                        ->max() ?? 0,
''',
          )

          # Public catalog: filter by own type OR published child type.
          replace_once(
              'app/Http/Controllers/Site/ObjectController.php',
              "            ->with(['objectType', 'vicariate', 'deanery', 'coverMedia', 'sanctities'])\n",
              "            ->with([\n                'objectType',\n                'vicariate',\n                'deanery',\n                'coverMedia',\n                'sanctities',\n                'parentObject.objectType',\n                'publishedChildObjects.objectType',\n                'publishedChildObjects.sanctities',\n            ])\n            ->withCount('publishedChildObjects')\n",
          )
          replace_once(
              'app/Http/Controllers/Site/ObjectController.php',
              '''            ->when($filters['type'] ?? null, function (Builder $query, string $slug) {
                $query->whereHas('objectType', fn (Builder $query) => $query->where('slug', $slug));
            })
''',
              "            ->typeOrParentOfType($filters['type'] ?? null)\n",
          )
          replace_once(
              'app/Http/Controllers/Site/ObjectController.php',
              "            'objectType',\n            'vicariate',\n",
              "            'objectType',\n            'parentObject.objectType',\n            'publishedChildObjects.objectType',\n            'vicariate',\n",
          )

          # API catalog and mobile clients get the same behavior.
          replace_once(
              'app/Http/Controllers/Api/V1/PilgrimageObjectController.php',
              "            ->with(['objectType', 'vicariate', 'deanery', 'coverMedia', 'sanctities'])\n",
              "            ->with([\n                'objectType',\n                'vicariate',\n                'deanery',\n                'coverMedia',\n                'sanctities',\n                'parentObject.objectType',\n                'publishedChildObjects.objectType',\n            ])\n            ->withCount('publishedChildObjects')\n",
          )
          replace_once(
              'app/Http/Controllers/Api/V1/PilgrimageObjectController.php',
              '''        $query->when($validated['type'] ?? null, function (Builder $query, string $slug) {
            $query->whereHas('objectType', function (Builder $query) use ($slug) {
                $query->where('slug', $slug);
            });
        });
''',
              "        $query->typeOrParentOfType($validated['type'] ?? null);\n",
          )
          replace_once(
              'app/Http/Controllers/Api/V1/PilgrimageObjectController.php',
              "            'objectType',\n            'vicariate',\n",
              "            'objectType',\n            'parentObject.objectType',\n            'publishedChildObjects.objectType',\n            'vicariate',\n",
          )

          # API resource exposes hierarchy without recursive resources.
          replace_once(
              'app/Http/Resources/PilgrimageObjectResource.php',
              '''            'deanery' => $this->whenLoaded('deanery', function () {
                return $this->deanery ? [
                    'id' => $this->deanery->id,
                    'name' => $this->deanery->name,
                    'slug' => $this->deanery->slug,
                ] : null;
            }),
''',
              '''            'deanery' => $this->whenLoaded('deanery', function () {
                return $this->deanery ? [
                    'id' => $this->deanery->id,
                    'name' => $this->deanery->name,
                    'slug' => $this->deanery->slug,
                ] : null;
            }),
            'parent_object' => $this->whenLoaded('parentObject', function () {
                return $this->parentObject ? [
                    'id' => $this->parentObject->id,
                    'slug' => $this->parentObject->slug,
                    'name' => $this->parentObject->name,
                    'type' => $this->parentObject->objectType ? [
                        'id' => $this->parentObject->objectType->id,
                        'name' => $this->parentObject->objectType->name,
                        'slug' => $this->parentObject->objectType->slug,
                    ] : null,
                ] : null;
            }),
            'child_objects' => $this->whenLoaded('publishedChildObjects', function () {
                return $this->publishedChildObjects->map(function ($child) {
                    return [
                        'id' => $child->id,
                        'slug' => $child->slug,
                        'name' => $child->name,
                        'type' => $child->objectType ? [
                            'id' => $child->objectType->id,
                            'name' => $child->objectType->name,
                            'slug' => $child->objectType->slug,
                        ] : null,
                    ];
                })->values();
            }),
            'child_objects_count' => $this->when(
                array_key_exists('published_child_objects_count', $this->getAttributes()),
                (int) ($this->published_child_objects_count ?? 0)
            ),
''',
          )

          # Admin controller: selection, validation, no cycles, hierarchy-aware search/filter.
          replace_once(
              'app/Http/Controllers/Admin/PilgrimageObjectController.php',
              "            ->with(['objectType', 'vicariate', 'deanery', 'coverMedia'])\n",
              "            ->with(['objectType', 'vicariate', 'deanery', 'coverMedia', 'parentObject.objectType'])\n            ->withCount('childObjects')\n",
          )
          old_admin_search = '''            ->when($filters['q'] ?? null, function (Builder $query, string $term) {
                $term = trim($term);
                $query->where(function (Builder $query) use ($term) {
                    $query->where('name', 'like', "%{$term}%")
                        ->orWhere('address', 'like', "%{$term}%");
                });
            })
            ->when($filters['type'] ?? null, fn (Builder $query, int $type) => $query->where('object_type_id', $type))
'''
          new_admin_search = '''            ->search($filters['q'] ?? null)
            ->when($filters['type'] ?? null, function (Builder $query, int $type) {
                $query->where(function (Builder $query) use ($type) {
                    $query->where('object_type_id', $type)
                        ->orWhereHas('childObjects', fn (Builder $query) => $query->where('object_type_id', $type));
                });
            })
'''
          replace_once('app/Http/Controllers/Admin/PilgrimageObjectController.php', old_admin_search, new_admin_search)
          replace_once(
              'app/Http/Controllers/Admin/PilgrimageObjectController.php',
              "        $object->load(['objectType', 'vicariate', 'deanery', 'sanctities', 'media', 'pointsOfInterest']);\n",
              "        $object->load(['objectType', 'parentObject.objectType', 'childObjects.objectType', 'vicariate', 'deanery', 'sanctities', 'media', 'pointsOfInterest']);\n",
          )
          replace_once(
              'app/Http/Controllers/Admin/PilgrimageObjectController.php',
              "        $object->load(['sanctities', 'media', 'pointsOfInterest']);\n",
              "        $object->load(['parentObject.objectType', 'childObjects.objectType', 'sanctities', 'media', 'pointsOfInterest']);\n",
          )
          replace_once(
              'app/Http/Controllers/Admin/PilgrimageObjectController.php',
              '''    private function formView(PilgrimageObject $object): View
    {
        return view('admin.objects.form', [
''',
              '''    private function formView(PilgrimageObject $object): View
    {
        $excludedParentIds = $object->exists
            ? array_merge([$object->id], $this->descendantIds($object))
            : [];

        return view('admin.objects.form', [
''',
          )
          replace_once(
              'app/Http/Controllers/Admin/PilgrimageObjectController.php',
              "            'types' => ObjectType::query()->orderBy('sort_order')->orderBy('name')->get(),\n",
              "            'types' => ObjectType::query()->orderBy('sort_order')->orderBy('name')->get(),\n            'parentObjects' => PilgrimageObject::query()\n                ->with('objectType')\n                ->when($excludedParentIds !== [], fn (Builder $query) => $query->whereNotIn('id', $excludedParentIds))\n                ->orderBy('name')\n                ->get(),\n",
          )
          replace_once(
              'app/Http/Controllers/Admin/PilgrimageObjectController.php',
              '''        return $request->validate([
            'object_type_id' => ['required', 'integer', 'exists:object_types,id'],
''',
              '''        $parentRules = [
            'nullable',
            'integer',
            Rule::exists('pilgrimage_objects', 'id')->whereNull('deleted_at'),
            function (string $attribute, mixed $value, $fail) use ($object) {
                if (! $object?->exists || ! $value) {
                    return;
                }

                $candidate = PilgrimageObject::query()->find((int) $value);
                $visited = [];

                while ($candidate) {
                    if ($candidate->id === $object->id) {
                        $fail('Нельзя выбрать сам объект или его дочерний объект в качестве родителя.');
                        return;
                    }

                    if (isset($visited[$candidate->id])) {
                        return;
                    }

                    $visited[$candidate->id] = true;
                    $candidate = $candidate->parentObject;
                }
            },
        ];

        return $request->validate([
            'object_type_id' => ['required', 'integer', 'exists:object_types,id'],
            'parent_object_id' => $parentRules,
''',
          )
          replace_once(
              'app/Http/Controllers/Admin/PilgrimageObjectController.php',
              '''    private function uniqueSlug(?string $slug, string $name, ?int $ignoreId = null): string
''',
              '''    /** @return array<int> */
    private function descendantIds(PilgrimageObject $object): array
    {
        $descendants = [];
        $frontier = [$object->id];

        while ($frontier !== []) {
            $frontier = PilgrimageObject::withTrashed()
                ->whereIn('parent_object_id', $frontier)
                ->pluck('id')
                ->map(fn ($id) => (int) $id)
                ->all();

            $newIds = array_values(array_diff($frontier, $descendants));
            if ($newIds === []) {
                break;
            }

            $descendants = array_values(array_unique(array_merge($descendants, $newIds)));
            $frontier = $newIds;
        }

        return $descendants;
    }

    private function uniqueSlug(?string $slug, string $name, ?int $ignoreId = null): string
''',
          )

          # Admin form field.
          replace_once(
              'resources/views/admin/objects/form.blade.php',
              '''                    <div class="col-12">
                        <label class="form-label" for="slug">Slug</label>
''',
              '''                    <div class="col-12">
                        <label class="form-label" for="parent_object_id">Родительский объект</label>
                        <select class="form-select" id="parent_object_id" name="parent_object_id">
                            <option value="">Нет — самостоятельный объект</option>
                            @foreach($parentObjects as $parentObject)
                                <option value="{{ $parentObject->id }}" @selected((string)old('parent_object_id', $object->parent_object_id) === (string)$parentObject->id)>
                                    {{ $parentObject->name }}@if($parentObject->objectType) — {{ $parentObject->objectType->name }}@endif
                                </option>
                            @endforeach
                        </select>
                        <div class="form-text">Для храма на территории монастыря выберите монастырь родительским объектом.</div>
                    </div>
                    <div class="col-12">
                        <label class="form-label" for="slug">Slug</label>
''',
          )

          # Admin list shows hierarchy.
          replace_once(
              'resources/views/admin/objects/index.blade.php',
              '''                                    <div class="small text-secondary text-truncate" style="max-width:320px">{{ $object->address }}</div>
                                    <code class="small">{{ $object->slug }}</code>
''',
              '''                                    <div class="small text-secondary text-truncate" style="max-width:320px">{{ $object->address }}</div>
                                    @if($object->parentObject)
                                        <div class="small text-secondary"><i class="bi bi-diagram-2 me-1"></i>В составе: {{ $object->parentObject->name }}</div>
                                    @elseif($object->child_objects_count > 0)
                                        <div class="small text-secondary"><i class="bi bi-diagram-3 me-1"></i>Дочерних объектов: {{ $object->child_objects_count }}</div>
                                    @endif
                                    <code class="small">{{ $object->slug }}</code>
''',
          )

          # Public card explains why a parent appears in temple results.
          replace_once(
              'resources/views/site/partials/object-card.blade.php',
              '''        <div class="object-meta mb-3"><i class="bi bi-geo-alt me-1"></i>{{ $object->address }}</div>
''',
              '''        <div class="object-meta mb-2"><i class="bi bi-geo-alt me-1"></i>{{ $object->address }}</div>
        @if($object->parentObject)
            <div class="small text-secondary mb-3"><i class="bi bi-diagram-2 me-1"></i>В составе: {{ $object->parentObject->name }}</div>
        @elseif(($object->published_child_objects_count ?? 0) > 0)
            <div class="small text-secondary mb-3"><i class="bi bi-diagram-3 me-1"></i>На территории: {{ $object->published_child_objects_count }} связанных объектов</div>
        @endif
''',
          )

          # Feature coverage.
          test = Path('tests/Feature/ObjectHierarchySearchTest.php')
          test.write_text(r'''<?php

namespace Tests\Feature;

use App\Models\ObjectType;
use App\Models\PilgrimageObject;
use Illuminate\Foundation\Testing\RefreshDatabase;
use Tests\TestCase;

class ObjectHierarchySearchTest extends TestCase
{
    use RefreshDatabase;

    public function test_temple_query_returns_temple_and_its_parent_monastery(): void
    {
        [$monastery, $temple] = $this->hierarchy();

        $this->get(route('objects.index', ['q' => 'храм']))
            ->assertOk()
            ->assertSee($temple->name)
            ->assertSee($monastery->name)
            ->assertSee('На территории: 1 связанных объектов');

        $this->getJson('/api/v1/objects?q=храм')
            ->assertOk()
            ->assertJsonFragment(['id' => $temple->id, 'name' => $temple->name])
            ->assertJsonFragment(['id' => $monastery->id, 'name' => $monastery->name]);
    }

    public function test_temple_type_filter_includes_parent_monastery(): void
    {
        [$monastery, $temple] = $this->hierarchy();

        $this->get(route('objects.index', ['type' => 'temple']))
            ->assertOk()
            ->assertSee($temple->name)
            ->assertSee($monastery->name);

        $this->getJson('/api/v1/objects?type=temple')
            ->assertOk()
            ->assertJsonFragment(['id' => $temple->id, 'name' => $temple->name])
            ->assertJsonFragment(['id' => $monastery->id, 'name' => $monastery->name]);
    }

    public function test_parent_and_child_relationships_are_available(): void
    {
        [$monastery, $temple] = $this->hierarchy();

        $this->assertTrue($temple->parentObject->is($monastery));
        $this->assertTrue($monastery->childObjects->contains($temple));
        $this->assertTrue($monastery->publishedChildObjects->contains($temple));
    }

    /** @return array{PilgrimageObject, PilgrimageObject} */
    private function hierarchy(): array
    {
        $monasteryType = ObjectType::query()->create([
            'name' => 'Монастырь',
            'slug' => 'monastery',
            'marker_color' => '#26443b',
            'icon' => 'bi-buildings',
            'sort_order' => 1,
        ]);
        $templeType = ObjectType::query()->create([
            'name' => 'Храм',
            'slug' => 'temple',
            'marker_color' => '#b08a3e',
            'icon' => 'bi-building',
            'sort_order' => 2,
        ]);

        $monastery = PilgrimageObject::query()->create([
            'object_type_id' => $monasteryType->id,
            'name' => 'Покровский монастырь',
            'slug' => 'pokrovskiy-monastyr',
            'address' => 'Москва, монастырская территория',
            'latitude' => 55.7558,
            'longitude' => 37.6176,
            'is_published' => true,
            'published_at' => now()->subMinute(),
        ]);
        $temple = PilgrimageObject::query()->create([
            'object_type_id' => $templeType->id,
            'parent_object_id' => $monastery->id,
            'name' => 'Храм Покрова на территории монастыря',
            'slug' => 'hram-pokrova-v-monastyre',
            'address' => 'Москва, территория Покровского монастыря',
            'latitude' => 55.7559,
            'longitude' => 37.6177,
            'is_published' => true,
            'published_at' => now()->subMinute(),
        ]);

        return [$monastery->fresh(), $temple->fresh()];
    }
}
''', encoding='utf-8')
          PY

      - name: Install PHP dependencies
        run: composer install --no-interaction --prefer-dist

      - name: Prepare SQLite tests
        run: |
          cp .env.example .env
          php artisan key:generate
          touch database/database.sqlite
          sed -i 's/^DB_CONNECTION=.*/DB_CONNECTION=sqlite/' .env
          sed -i 's|^DB_DATABASE=.*|DB_DATABASE='"$GITHUB_WORKSPACE"'/database/database.sqlite|' .env

      - name: Verify PHP and Blade
        run: |
          php -l app/Models/PilgrimageObject.php
          php -l app/Services/PilgrimageObjectSearchService.php
          php -l app/Services/PilgrimageObjectFuzzySearch.php
          php -l app/Http/Controllers/Admin/PilgrimageObjectController.php
          php -l app/Http/Controllers/Site/ObjectController.php
          php -l app/Http/Controllers/Api/V1/PilgrimageObjectController.php
          php -l app/Http/Resources/PilgrimageObjectResource.php
          php -l database/migrations/2026_07_31_124500_add_parent_object_id_to_pilgrimage_objects_table.php
          php -l tests/Feature/ObjectHierarchySearchTest.php
          php artisan view:cache

      - name: Run hierarchy tests
        run: php artisan test --filter=ObjectHierarchySearchTest

      - name: Commit implementation
        run: |
          rm -f .github/workflows/add-object-hierarchy.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat(objects): add parent hierarchy and inherited search"
          git push origin HEAD:main
